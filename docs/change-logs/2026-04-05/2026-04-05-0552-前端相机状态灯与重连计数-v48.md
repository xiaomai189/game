# Change Log - 2026-04-05 05:52 - 前端相机状态灯与重连计数（v48）

## 改动内容
- `web/index.html`
  - 在控制面板下新增 `camera-health-grid`，默认展示 `cam0/cam1` 两张状态卡。
- `web/styles.css`
  - 新增相机状态卡样式与状态灯色彩映射：
    - `ok` 绿色
    - `warmup` 黄色
    - `black` 粉红
    - `reconnecting` 蓝色
    - `offline/unknown` 灰紫
- `web/game/index.js`
  - 新增 `state.cameraRuntimeById`，维护 `cam0/cam1` 运行态。
  - 新增 `normalizeCameraRuntime` 与状态渲染函数，处理字段容错：
    - `cameraStatus`
    - `cameraStatusReason`
    - `reconnectAttempts`
    - `blackFrameStreak`
  - `onPose` 解析 `payload.cameras[]` 并刷新相机状态卡。
  - `render_game_to_text` 新增 `cameraStatus` 摘要，便于自动化读取。
  - 诊断行追加 `cam0/cam1` 状态与重连计数。
- `scripts/test-web-dual-race.mjs`
  - 新增断言：`render_game_to_text().cameraStatus` 存在并为 `OK`。
  - 新增断言：`#camera0Status` UI 文本包含状态值。

## 变更原因
- 双摄联调时仅看长文本不够直观，需要快速定位异常相机和重连行为。
- 后端已产出状态字段，前端应把这些字段可视化用于现场排障。

## 验证结果
- `npm run build`：通过
- `npm run test:web-dual-race`：通过
- `python -m pytest -q`：通过（40 passed）
- Playwright 客户端动作回放已执行（`develop-web-game` 流程）

## UI 证据
- 页面截图：`docs/evidence/2026-04-05/2026-04-05-0545-v48-camera-status-ui.png`
- Playwright 产物目录：`docs/evidence/2026-04-05/v48-playwright/`
