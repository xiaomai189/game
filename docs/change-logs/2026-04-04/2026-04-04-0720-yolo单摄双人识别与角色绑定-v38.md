# Change Log - 2026-04-04 07:20 - YOLO 单摄双人识别与角色绑定（v38）

## 改动内容
- `core/pose_engine.py`
  - 新增 `PosePerson` 与扩展 `PoseOutput`（`persons`、`primary_track_id`）。
  - 在保持原主目标 `keypoints` 输出的前提下，新增最多 2 人的多人输出。
  - 新增 `_select_top_person_indices`，用于 top-N 人选取。
- `core/action_engine.py`
  - 新增 `PersonActionState`、`MultiActionOutput`、`DualRoleBinder`。
  - 新增 `infer_multi(...)`：逐人动作推理 + P1/P2 绑定 + `primary_action` 回退。
  - 保留既有 `infer(...)` 单人接口不变。
- `core/web_payload.py`
  - 增量新增 `persons[]` 与 `roles{p1,p2}` 输出。
  - 旧字段 `actions/hands/body` 保持兼容。
- `config.py` / `main.py`
  - 新增 v38 配置项：`max_persons`、`person_select_policy`、`dual_role_enabled`、`role_bind_grace_ms`。
  - 新增 CLI 参数：`--max-persons`、`--role-bind-grace-ms`、`--disable-dual-role`。
  - 主循环接入 `infer_multi`，并把多人结构发布到 websocket payload。
- 测试
  - `tests/test_pose_engine.py`：新增 top-person 选择回归。
  - `tests/test_action_engine.py`：新增双人角色绑定与 grace 行为回归。
  - `tests/test_web_payload.py`：新增多人 payload 输出回归。

## 改动原因
- 先交付单摄双人可观测能力，同时保证现有单人链路和 web 游戏兼容运行。
- 为后续多摄像头“独立通道并行”奠定统一数据结构。

## 验证结果
- `.\.venv\Scripts\python.exe -m pytest -q`：通过（27 passed）
- `npm run build`：通过
- `npm run preflight`：通过（pytest + web 回归全通过）

## 兼容性说明
- 老字段未删除；前端若只消费旧字段，行为保持不变。
- 多人信息作为增量字段输出，不影响现有协议消费方。
