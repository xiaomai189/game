# 变更记录 - 2026-04-04 22:18 - Auto 模式连接修复（v45）

## 基本信息
- 日期：2026-04-04 22:18
- 任务/PRD：`docs/rpd/2026-04-04-2217-auto模式连接修复-v45.md`
- 负责人：Codex

## 改动内容
- `web/game/index.js`
  - 移除 `Auto + 非 RUNNING` 时主动断开 WS 的逻辑。
  - Auto 模式下与 Camera 模式一致，持续维持 WS 连接尝试。
- `scripts/stop-oneclick.ps1`
  - 增加仓库级强清理：兜底结束本仓库内 `main.py` 与 `http.server` 相关 python 进程。

## 改动原因
- 解决用户在 READY 状态误判“前端连不上 YOLO”的问题。
- 解决残留重复进程导致状态混乱、端口冲突、测试不稳定的问题。

## 验证结果
- `npm run build`：通过。
- `npm run test:web-input-mode-fallback`：通过。
- `npm run test:web-ws-reconnect-noise`：通过。
- Playwright 实测：`Input=Auto` + `Mode=Auto` + `Status=READY` 下显示 `Pose stream connected`。

## 界面证据
- `docs/evidence/2026-04-04/2026-04-04-2214-auto-mode-ws-connected-v45.png`

## 备注
- `npm run build` 已执行。
