# 变更记录 - 2026-04-04 19:50 - 双摄自检与自动降级（v44）

## 基本信息
- 日期：2026-04-04 19:50
- 任务/PRD：`docs/rpd/2026-04-04-1933-双摄自检与自动降级-v44.md`
- 负责人：Codex

## 改动内容
- 后端新增双摄守护能力：
  - 启动自检窗口（`--self-check-sec`）；
  - 运行期降级/恢复状态机（自动降级可开关）；
  - 双摄健康评估（render/infer/latency/health score）。
- 后端新增阈值参数与 CLI 覆盖：
  - `--dual-min-fps`
  - `--max-input-age-ms`
  - `--auto-degrade true|false`
- WebSocket payload 增强：
  - 根节点新增 `runtime.dualRequested/dualReady/selfCheckStatus/reason/degradedCameraIds`。
  - 每路 camera pipeline 新增 `healthLevel/healthReason`。
- 前端自动模式切换收敛：
  - 自动进入 dual race 需满足 `runtime.dualReady=true`；
  - 诊断栏新增 dual/self-check 状态显示。
- 新增单元测试：
  - `tests/test_dual_runtime_guard.py` 覆盖健康评估与降级/恢复逻辑。

## 改动原因
- 解决“2P 看起来未连接但 ws 仍在线”的误判问题。
- 将“性能不足”从主观体验变成可观测、可降级、可恢复的稳定机制。

## 验证结果
- 构建：通过（`npm run build`）。
- Python 测试：通过（`pytest -q`，36 passed）。
- 前端关键路径：
  - `npm run test:web-dual-race` 通过；
  - `npm run test:web-ws-reconnect-noise` 通过（清理端口占用后执行）。
- 运行验证：
  - `logs/perf-v44-single.json`：单摄正常；
  - `logs/perf-v44-dual.json`：双摄低帧时 `dualReady=false`，`selfCheckStatus=degraded`，符合预期降级。

## 备注
- `npm run build`：已执行并通过。
- UI 证据：`docs/evidence/2026-04-04/2026-04-04-1949-dual-runtime-guard-v44.md`
