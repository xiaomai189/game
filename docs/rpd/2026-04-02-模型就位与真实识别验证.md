# PRD：模型就位与真实识别验证

## 背景
- 当前摄像头链路已可运行，但 `models/yolo11n-pose.pt` 缺失，导致姿态识别功能未启用。

## 目标
- 下载并放置 `yolo11n-pose.pt` 到 `models/` 目录。
- 验证程序可在真实摄像头模式下加载模型并运行。

## 范围
- In Scope：
  - 下载模型文件并校验存在。
  - 运行主程序（有限帧）验证模型加载链路。
  - 输出一张真实识别运行证据图。
- Out of Scope：
  - 不改动动作算法与玩法逻辑。
  - 不进行模型替换或训练。

## 方案
- 使用 `ultralytics` 官方下载流程获取 `yolo11n-pose.pt`。
- 文件保存到 `models/yolo11n-pose.pt`。
- 运行 `main.py` 生成运行截图并确认不再提示“Model file not found”。

## 验收标准
- [ ] `models/yolo11n-pose.pt` 存在且可被程序加载。
- [ ] 真实摄像头模式可运行并生成证据图。
- [ ] 变更记录完成。

## 风险与缓解
- 风险：网络波动导致下载失败。
- 缓解：重试下载并校验文件存在与大小。
